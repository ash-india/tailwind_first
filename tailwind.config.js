/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./distribution/*.html"],   //all the html files will be handled from here, when '*' is there, otherwise index.html
  theme: {
    extend: {},
  },
  plugins: [],
}

